# Erdős #269: conditional carry-lift extinction

This Comparator package exposes six exact interfaces used by the three-prime
programme, ordered by mathematical significance rather than source-file or
alphabetical order.

First, a nonzero integral lift error is impossible as soon as the induced
perturbation is block-null, vanishes at genuine `2 → 3` and `2 → 5` anchors,
every integer base is at least two, and its error bound drops below `2^N` at
even one index.  The mechanism is exact: two-anchor rigidity extinguishes the
perturbation, after which the nonzero lift error grows by the product of all
preceding bases.  A separate four-state theorem gives a sharp finite
obstruction: unit-accurate integral approximants cannot satisfy both anchors
and a null first complete `2`-block.

The package also exposes the load-bearing two-anchor extinction theorem and
the exact weighted block-defect identity, which records both endpoint errors
and the interior weighted-error term that must be controlled before ordinary
block-nullity can be inferred.  Finally it retains the named
`CofinalLocalWindowEscape` consumer and the exact decomposition of an integral
carry into a residue digit plus an uncontrolled integral coboundary.

## Boundary and disposition

No theorem here constructs the actual ordered-power orbit or an integral lift,
proves its perturbation block-null, or supplies genuine `2 → 3` and `2 → 5`
zero anchors.  `CofinalLocalWindowEscape` is likewise a hypothesis, and the
rationality-to-carry bridge remains open.  Therefore the package does not
prove irrationality in Erdős #269.  It identifies several exact arithmetic
interfaces a future argument can target and verifies their consumers without
hiding the antecedents.

The four-theorem extinction family is mathematically coherent and potentially
note-grade even while the parent problem remains open.  Its Palomar disposition
must therefore be assessed on research interest and source identity rather
than dismissed merely as a helper package.  The unconditional structure entry
still precedes it because it has no lift or block-nullity antecedent.
