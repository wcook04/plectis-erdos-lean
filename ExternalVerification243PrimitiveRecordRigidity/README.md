# Primitive record-rise-two rigidity

Along a dynamically reduced reciprocal tail with arbitrary cancellation, a prime power in the reduced denominator cannot drop while the raw numerator stays below the next power; it persists up to a first height; an odd multiple of that prime above the running numerator maximum is unreachable under record jumps of size at most two; a large odd prime power supplies such a trap; a vanishing centred error forces a unit numerator; two consecutive zeros pin the Sylvester step; and, given a late fresh odd prime-power supply, the multipliers eventually follow a ↦ a² - a + 1. The Challenge imports only Mathlib and restates `runningMax` and `sylvesterNext`. The Solution transports the eight audited source endpoints from `ErdosProblems.Erdos243.PrimitiveRecordBarrier`.

The composed consumer is conditional on its record-jump bound and prime-power supply. It does not settle Erdős #243. Novelty is unassessed. No submission has occurred.
