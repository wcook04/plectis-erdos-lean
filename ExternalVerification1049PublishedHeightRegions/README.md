# Published height-region placement at 3/2

For a reduced rational base a/b, two elementary parameter regions are fixed: the published Bundschuh--Väänänen inequality log b / log a < 1/2 - 1/π², and the threshold log b / log a < 81/200. The integer obstruction 3^81 < 2^200 places log 2 / log 3 strictly above 81/200, so 3/2 lies outside both regions. The Challenge imports only Mathlib, restates both region predicates, and assumes only those four placements. The Solution transports the audited source endpoints from `ErdosProblems.Erdos1049.ZudilinHeightRegion`.

This is method inapplicability at 3/2, not rationality, irrationality, or an internalized analytic theorem. Novelty is unassessed. No submission has occurred.
