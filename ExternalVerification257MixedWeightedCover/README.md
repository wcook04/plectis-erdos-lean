# ExternalVerification257MixedWeightedCover

A binary finite-prime-weighted host and a strengthened positive-cover host
together force all-base hereditary irrationality of every infinite subset of
their union. The mixed statement uses one common observation scheme. It does
not settle Erdős #257.

`Challenge.lean` imports only Mathlib and restates `MixedSupportClaim`.
`Solution.lean` imports `WeightedReturn` and cites `mixedSupportClaim`.
