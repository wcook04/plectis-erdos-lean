# ExternalVerification257VariableExponentCover

Every infinite subset of a strengthened positive-cover host is irrational at
every integer base at least two. The cover data retain variable exponents,
column summability, divisor majorisation, and the one-inverse-power cost.
This does not settle Erdős #257.

`Challenge.lean` imports only Mathlib and restates
`StrengthenedPositiveCoverClaim`. `Solution.lean` imports
`PositiveCoverReturn` and cites `strengthenedPositiveCoverClaim`.
